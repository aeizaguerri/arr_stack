---
services:

################
# RADARR
################

  radarr:
    image: lscr.io/linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - ${MY_PATH}/radarr/data:/config
      - ${MY_PATH}/movies:/movies #optional
      - ${MY_PATH}/download-client-downloads:/downloads #optional
    ports:
      - 7878:7878
    restart: unless-stopped

################
# SONARR
################

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - ${MY_PATH}/sonarr/data:/config
      - ${MY_PATH}/tvseries:/tv #optional
      - ${MY_PATH}/downloadclient-downloads:/downloads #optional
    ports:
      - 8989:8989
    restart: unless-stopped

################
# PROWLARR
################

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    container_name: prowlarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - ${MY_PATH}/prowlarr/data:/config
    ports:
      - 9696:9696
    restart: unless-stopped

#### QBITTORRENT ####
  qbittorrent:
    image: lscr.io/linuxserver/qbittorrent:latest
    container_name: qbittorrent
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
      - WEBUI_PORT=8080
      - TORRENTING_PORT=6881
    volumes:
      - ${MY_PATH}/qbittorrent/appdata:/config
      - ${MY_PATH}/downloads:/downloads #optional
    ports:
      - 8080:8080
      - 6881:6881
      - 6881:6881/udp
    restart: unless-stopped

#### JELLYFIN ####
  jellyfin:
    image: lscr.io/linuxserver/jellyfin:latest
    container_name: jellyfin
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Etc/UTC
    volumes:
      - ${MY_PATH}/jellyfin/library:/config
      - ${MY_PATH}/tvseries:/data/tvshows
      - ${MY_PATH}/movies:/data/movies
    ports:
      - 8096:8096
      - 8920:8920 #optional
      - 7359:7359/udp #optional
      - 1900:1900/udp #optional
    restart: unless-stopped